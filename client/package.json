{
  "name": "sawtooth-reason-supply-client",
  "version": "0.1.0",
  "scripts": {
    "start": "bsb -make-world && razzle start",
    "dev": "bsb -make-world && razzle start",
    "clean": "bsb -clean-world && rm -rf lib build && find . -type f -name '*.bs.js' -delete",
    "build": "bsb -make-world && razzle build",
    "test": "razzle test --env=jsdom"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "reason-react": "^0.7.0"
  },
  "devDependencies": {
    "bs-platform": "7.0.1",
    "razzle": "^3.0.0"
  }
}
